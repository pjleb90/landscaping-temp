/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.unsplash.com",
            "raw.githubusercontent.com",
        ]
    }
}

module.exports = nextConfig
